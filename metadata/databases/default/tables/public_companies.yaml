table:
  name: companies
  schema: public
array_relationships:
  - name: company_rooms
    using:
      foreign_key_constraint_on:
        column: company_id
        table:
          name: companies_rooms
          schema: public
  - name: invoices
    using:
      manual_configuration:
        column_mapping:
          id: company_id
        insertion_order: null
        remote_table:
          name: invoices
          schema: public
  - name: users
    using:
      manual_configuration:
        column_mapping:
          id: company_id
        insertion_order: null
        remote_table:
          name: users
          schema: public
